/*:
 * @plugindesc Allows developers to set the camera to move based on what regions the player moves onto.
 * @author SumRndmDde
 *
 * @param Reset Duration
 * @desc The duration used to reset the zoom back to normal when the player steps on a tile that is not a "camera region".
 * @default 30
 *
 * @param == Camera Region 1 ==
 * @default
 *
 * @param Region 1 ID
 * @desc The ID of the region that will activate "Camera Region 1"
 * @default
 *
 * @param Region 1 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 1 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 1 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 2 ==
 * @default
 *
 * @param Region 2 ID
 * @desc The ID of the region that will activate "Camera Region 2"
 * @default
 *
 * @param Region 2 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 2 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 2 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 3 ==
 * @default
 *
 * @param Region 3 ID
 * @desc The ID of the region that will activate "Camera Region 3"
 * @default
 *
 * @param Region 3 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 3 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 3 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 4 ==
 * @default
 *
 * @param Region 4 ID
 * @desc The ID of the region that will activate "Camera Region 4"
 * @default
 *
 * @param Region 4 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 4 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 4 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 5 ==
 * @default
 *
 * @param Region 5 ID
 * @desc The ID of the region that will activate "Camera Region 5"
 * @default
 *
 * @param Region 5 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 5 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 5 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 6 ==
 * @default
 *
 * @param Region 6 ID
 * @desc The ID of the region that will activate "Camera Region 6"
 * @default
 *
 * @param Region 6 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 6 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 6 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 7 ==
 * @default
 *
 * @param Region 7 ID
 * @desc The ID of the region that will activate "Camera Region 7"
 * @default
 *
 * @param Region 7 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 7 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 7 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 8 ==
 * @default
 *
 * @param Region 8 ID
 * @desc The ID of the region that will activate "Camera Region 8"
 * @default
 *
 * @param Region 8 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 8 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 8 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 9 ==
 * @default
 *
 * @param Region 9 ID
 * @desc The ID of the region that will activate "Camera Region 9"
 * @default
 *
 * @param Region 9 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 9 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 9 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 10 ==
 * @default
 *
 * @param Region 10 ID
 * @desc The ID of the region that will activate "Camera Region 10"
 * @default
 *
 * @param Region 10 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 10 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 10 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 11 ==
 * @default
 *
 * @param Region 11 ID
 * @desc The ID of the region that will activate "Camera Region 11"
 * @default
 *
 * @param Region 11 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 11 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 11 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 12 ==
 * @default
 *
 * @param Region 12 ID
 * @desc The ID of the region that will activate "Camera Region 12"
 * @default
 *
 * @param Region 12 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 12 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 12 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 13 ==
 * @default
 *
 * @param Region 13 ID
 * @desc The ID of the region that will activate "Camera Region 13"
 * @default
 *
 * @param Region 13 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 13 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 13 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 14 ==
 * @default
 *
 * @param Region 14 ID
 * @desc The ID of the region that will activate "Camera Region 14"
 * @default
 *
 * @param Region 14 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 14 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 14 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 15 ==
 * @default
 *
 * @param Region 15 ID
 * @desc The ID of the region that will activate "Camera Region 15"
 * @default
 *
 * @param Region 15 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 15 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 15 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 16 ==
 * @default
 *
 * @param Region 16 ID
 * @desc The ID of the region that will activate "Camera Region 16"
 * @default
 *
 * @param Region 16 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 16 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 16 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 17 ==
 * @default
 *
 * @param Region 17 ID
 * @desc The ID of the region that will activate "Camera Region 17"
 * @default
 *
 * @param Region 17 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 17 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 17 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 18 ==
 * @default
 *
 * @param Region 18 ID
 * @desc The ID of the region that will activate "Camera Region 18"
 * @default
 *
 * @param Region 18 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 18 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 18 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 19 ==
 * @default
 *
 * @param Region 19 ID
 * @desc The ID of the region that will activate "Camera Region 19"
 * @default
 *
 * @param Region 19 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 19 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 19 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 20 ==
 * @default
 *
 * @param Region 20 ID
 * @desc The ID of the region that will activate "Camera Region 20"
 * @default
 *
 * @param Region 20 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 20 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 20 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 21 ==
 * @default
 *
 * @param Region 21 ID
 * @desc The ID of the region that will activate "Camera Region 21"
 * @default
 *
 * @param Region 21 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 21 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 21 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 22 ==
 * @default
 *
 * @param Region 22 ID
 * @desc The ID of the region that will activate "Camera Region 22"
 * @default
 *
 * @param Region 22 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 22 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 22 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 23 ==
 * @default
 *
 * @param Region 23 ID
 * @desc The ID of the region that will activate "Camera Region 23"
 * @default
 *
 * @param Region 23 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 23 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 23 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 24 ==
 * @default
 *
 * @param Region 24 ID
 * @desc The ID of the region that will activate "Camera Region 24"
 * @default
 *
 * @param Region 24 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 24 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 24 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 25 ==
 * @default
 *
 * @param Region 25 ID
 * @desc The ID of the region that will activate "Camera Region 25"
 * @default
 *
 * @param Region 25 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 25 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 25 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 26 ==
 * @default
 *
 * @param Region 26 ID
 * @desc The ID of the region that will activate "Camera Region 26"
 * @default
 *
 * @param Region 26 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 26 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 26 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 27 ==
 * @default
 *
 * @param Region 27 ID
 * @desc The ID of the region that will activate "Camera Region 27"
 * @default
 *
 * @param Region 27 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 27 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 27 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 28 ==
 * @default
 *
 * @param Region 28 ID
 * @desc The ID of the region that will activate "Camera Region 28"
 * @default
 *
 * @param Region 28 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 28 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 28 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 29 ==
 * @default
 *
 * @param Region 29 ID
 * @desc The ID of the region that will activate "Camera Region 29"
 * @default
 *
 * @param Region 29 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 29 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 29 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 30 ==
 * @default
 *
 * @param Region 30 ID
 * @desc The ID of the region that will activate "Camera Region 30"
 * @default
 *
 * @param Region 30 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 30 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 30 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 31 ==
 * @default
 *
 * @param Region 31 ID
 * @desc The ID of the region that will activate "Camera Region 31"
 * @default
 *
 * @param Region 31 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 31 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 31 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 32 ==
 * @default
 *
 * @param Region 32 ID
 * @desc The ID of the region that will activate "Camera Region 32"
 * @default
 *
 * @param Region 32 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 32 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 32 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 33 ==
 * @default
 *
 * @param Region 33 ID
 * @desc The ID of the region that will activate "Camera Region 33"
 * @default
 *
 * @param Region 33 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 33 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 33 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 34 ==
 * @default
 *
 * @param Region 34 ID
 * @desc The ID of the region that will activate "Camera Region 34"
 * @default
 *
 * @param Region 34 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 34 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 34 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 35 ==
 * @default
 *
 * @param Region 35 ID
 * @desc The ID of the region that will activate "Camera Region 35"
 * @default
 *
 * @param Region 35 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 35 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 35 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 36 ==
 * @default
 *
 * @param Region 36 ID
 * @desc The ID of the region that will activate "Camera Region 36"
 * @default
 *
 * @param Region 36 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 36 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 36 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 37 ==
 * @default
 *
 * @param Region 37 ID
 * @desc The ID of the region that will activate "Camera Region 37"
 * @default
 *
 * @param Region 37 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 37 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 37 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 38 ==
 * @default
 *
 * @param Region 38 ID
 * @desc The ID of the region that will activate "Camera Region 38"
 * @default
 *
 * @param Region 38 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 38 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 38 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 39 ==
 * @default
 *
 * @param Region 39 ID
 * @desc The ID of the region that will activate "Camera Region 39"
 * @default
 *
 * @param Region 39 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 39 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 39 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 40 ==
 * @default
 *
 * @param Region 40 ID
 * @desc The ID of the region that will activate "Camera Region 40"
 * @default
 *
 * @param Region 40 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 40 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 40 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 41 ==
 * @default
 *
 * @param Region 41 ID
 * @desc The ID of the region that will activate "Camera Region 41"
 * @default
 *
 * @param Region 41 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 41 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 41 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 42 ==
 * @default
 *
 * @param Region 42 ID
 * @desc The ID of the region that will activate "Camera Region 42"
 * @default
 *
 * @param Region 42 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 42 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 42 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 43 ==
 * @default
 *
 * @param Region 43 ID
 * @desc The ID of the region that will activate "Camera Region 43"
 * @default
 *
 * @param Region 43 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 43 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 43 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 44 ==
 * @default
 *
 * @param Region 44 ID
 * @desc The ID of the region that will activate "Camera Region 44"
 * @default
 *
 * @param Region 44 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 44 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 44 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 45 ==
 * @default
 *
 * @param Region 45 ID
 * @desc The ID of the region that will activate "Camera Region 45"
 * @default
 *
 * @param Region 45 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 45 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 45 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 46 ==
 * @default
 *
 * @param Region 46 ID
 * @desc The ID of the region that will activate "Camera Region 46"
 * @default
 *
 * @param Region 46 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 46 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 46 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 47 ==
 * @default
 *
 * @param Region 47 ID
 * @desc The ID of the region that will activate "Camera Region 47"
 * @default
 *
 * @param Region 47 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 47 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 47 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 48 ==
 * @default
 *
 * @param Region 48 ID
 * @desc The ID of the region that will activate "Camera Region 48"
 * @default
 *
 * @param Region 48 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 48 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 48 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 49 ==
 * @default
 *
 * @param Region 49 ID
 * @desc The ID of the region that will activate "Camera Region 49"
 * @default
 *
 * @param Region 49 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 49 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 49 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 50 ==
 * @default
 *
 * @param Region 50 ID
 * @desc The ID of the region that will activate "Camera Region 50"
 * @default
 *
 * @param Region 50 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 50 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 50 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 51 ==
 * @default
 *
 * @param Region 51 ID
 * @desc The ID of the region that will activate "Camera Region 51"
 * @default
 *
 * @param Region 51 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 51 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 51 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 52 ==
 * @default
 *
 * @param Region 52 ID
 * @desc The ID of the region that will activate "Camera Region 52"
 * @default
 *
 * @param Region 52 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 52 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 52 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 53 ==
 * @default
 *
 * @param Region 53 ID
 * @desc The ID of the region that will activate "Camera Region 53"
 * @default
 *
 * @param Region 53 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 53 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 53 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 54 ==
 * @default
 *
 * @param Region 54 ID
 * @desc The ID of the region that will activate "Camera Region 54"
 * @default
 *
 * @param Region 54 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 54 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 54 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 55 ==
 * @default
 *
 * @param Region 55 ID
 * @desc The ID of the region that will activate "Camera Region 55"
 * @default
 *
 * @param Region 55 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 55 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 55 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 56 ==
 * @default
 *
 * @param Region 56 ID
 * @desc The ID of the region that will activate "Camera Region 56"
 * @default
 *
 * @param Region 56 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 56 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 56 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 57 ==
 * @default
 *
 * @param Region 57 ID
 * @desc The ID of the region that will activate "Camera Region 57"
 * @default
 *
 * @param Region 57 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 57 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 57 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 58 ==
 * @default
 *
 * @param Region 58 ID
 * @desc The ID of the region that will activate "Camera Region 58"
 * @default
 *
 * @param Region 58 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 58 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 58 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 59 ==
 * @default
 *
 * @param Region 59 ID
 * @desc The ID of the region that will activate "Camera Region 59"
 * @default
 *
 * @param Region 59 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 59 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 59 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 60 ==
 * @default
 *
 * @param Region 60 ID
 * @desc The ID of the region that will activate "Camera Region 60"
 * @default
 *
 * @param Region 60 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 60 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 60 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 61 ==
 * @default
 *
 * @param Region 61 ID
 * @desc The ID of the region that will activate "Camera Region 61"
 * @default
 *
 * @param Region 61 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 61 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 61 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 62 ==
 * @default
 *
 * @param Region 62 ID
 * @desc The ID of the region that will activate "Camera Region 62"
 * @default
 *
 * @param Region 62 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 62 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 62 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 63 ==
 * @default
 *
 * @param Region 63 ID
 * @desc The ID of the region that will activate "Camera Region 63"
 * @default
 *
 * @param Region 63 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 63 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 63 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 64 ==
 * @default
 *
 * @param Region 64 ID
 * @desc The ID of the region that will activate "Camera Region 64"
 * @default
 *
 * @param Region 64 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 64 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 64 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 65 ==
 * @default
 *
 * @param Region 65 ID
 * @desc The ID of the region that will activate "Camera Region 65"
 * @default
 *
 * @param Region 65 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 65 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 65 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 66 ==
 * @default
 *
 * @param Region 66 ID
 * @desc The ID of the region that will activate "Camera Region 66"
 * @default
 *
 * @param Region 66 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 66 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 66 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 67 ==
 * @default
 *
 * @param Region 67 ID
 * @desc The ID of the region that will activate "Camera Region 67"
 * @default
 *
 * @param Region 67 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 67 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 67 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 68 ==
 * @default
 *
 * @param Region 68 ID
 * @desc The ID of the region that will activate "Camera Region 68"
 * @default
 *
 * @param Region 68 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 68 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 68 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 69 ==
 * @default
 *
 * @param Region 69 ID
 * @desc The ID of the region that will activate "Camera Region 69"
 * @default
 *
 * @param Region 69 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 69 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 69 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 70 ==
 * @default
 *
 * @param Region 70 ID
 * @desc The ID of the region that will activate "Camera Region 70"
 * @default
 *
 * @param Region 70 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 70 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 70 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 71 ==
 * @default
 *
 * @param Region 71 ID
 * @desc The ID of the region that will activate "Camera Region 71"
 * @default
 *
 * @param Region 71 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 71 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 71 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 72 ==
 * @default
 *
 * @param Region 72 ID
 * @desc The ID of the region that will activate "Camera Region 72"
 * @default
 *
 * @param Region 72 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 72 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 72 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 73 ==
 * @default
 *
 * @param Region 73 ID
 * @desc The ID of the region that will activate "Camera Region 73"
 * @default
 *
 * @param Region 73 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 73 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 73 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 74 ==
 * @default
 *
 * @param Region 74 ID
 * @desc The ID of the region that will activate "Camera Region 74"
 * @default
 *
 * @param Region 74 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 74 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 74 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 75 ==
 * @default
 *
 * @param Region 75 ID
 * @desc The ID of the region that will activate "Camera Region 75"
 * @default
 *
 * @param Region 75 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 75 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 75 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 76 ==
 * @default
 *
 * @param Region 76 ID
 * @desc The ID of the region that will activate "Camera Region 76"
 * @default
 *
 * @param Region 76 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 76 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 76 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 77 ==
 * @default
 *
 * @param Region 77 ID
 * @desc The ID of the region that will activate "Camera Region 77"
 * @default
 *
 * @param Region 77 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 77 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 77 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 78 ==
 * @default
 *
 * @param Region 78 ID
 * @desc The ID of the region that will activate "Camera Region 78"
 * @default
 *
 * @param Region 78 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 78 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 78 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 79 ==
 * @default
 *
 * @param Region 79 ID
 * @desc The ID of the region that will activate "Camera Region 79"
 * @default
 *
 * @param Region 79 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 79 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 79 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 80 ==
 * @default
 *
 * @param Region 80 ID
 * @desc The ID of the region that will activate "Camera Region 80"
 * @default
 *
 * @param Region 80 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 80 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 80 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 81 ==
 * @default
 *
 * @param Region 81 ID
 * @desc The ID of the region that will activate "Camera Region 81"
 * @default
 *
 * @param Region 81 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 81 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 81 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 82 ==
 * @default
 *
 * @param Region 82 ID
 * @desc The ID of the region that will activate "Camera Region 82"
 * @default
 *
 * @param Region 82 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 82 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 82 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 83 ==
 * @default
 *
 * @param Region 83 ID
 * @desc The ID of the region that will activate "Camera Region 83"
 * @default
 *
 * @param Region 83 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 83 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 83 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 84 ==
 * @default
 *
 * @param Region 84 ID
 * @desc The ID of the region that will activate "Camera Region 84"
 * @default
 *
 * @param Region 84 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 84 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 84 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 85 ==
 * @default
 *
 * @param Region 85 ID
 * @desc The ID of the region that will activate "Camera Region 85"
 * @default
 *
 * @param Region 85 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 85 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 85 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 86 ==
 * @default
 *
 * @param Region 86 ID
 * @desc The ID of the region that will activate "Camera Region 86"
 * @default
 *
 * @param Region 86 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 86 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 86 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 87 ==
 * @default
 *
 * @param Region 87 ID
 * @desc The ID of the region that will activate "Camera Region 87"
 * @default
 *
 * @param Region 87 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 87 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 87 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 88 ==
 * @default
 *
 * @param Region 88 ID
 * @desc The ID of the region that will activate "Camera Region 88"
 * @default
 *
 * @param Region 88 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 88 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 88 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 89 ==
 * @default
 *
 * @param Region 89 ID
 * @desc The ID of the region that will activate "Camera Region 89"
 * @default
 *
 * @param Region 89 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 89 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 89 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 90 ==
 * @default
 *
 * @param Region 90 ID
 * @desc The ID of the region that will activate "Camera Region 90"
 * @default
 *
 * @param Region 90 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 90 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 90 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 91 ==
 * @default
 *
 * @param Region 91 ID
 * @desc The ID of the region that will activate "Camera Region 91"
 * @default
 *
 * @param Region 91 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 91 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 91 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 92 ==
 * @default
 *
 * @param Region 92 ID
 * @desc The ID of the region that will activate "Camera Region 92"
 * @default
 *
 * @param Region 92 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 92 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 92 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 93 ==
 * @default
 *
 * @param Region 93 ID
 * @desc The ID of the region that will activate "Camera Region 93"
 * @default
 *
 * @param Region 93 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 93 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 93 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 94 ==
 * @default
 *
 * @param Region 94 ID
 * @desc The ID of the region that will activate "Camera Region 94"
 * @default
 *
 * @param Region 94 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 94 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 94 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 95 ==
 * @default
 *
 * @param Region 95 ID
 * @desc The ID of the region that will activate "Camera Region 95"
 * @default
 *
 * @param Region 95 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 95 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 95 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 96 ==
 * @default
 *
 * @param Region 96 ID
 * @desc The ID of the region that will activate "Camera Region 96"
 * @default
 *
 * @param Region 96 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 96 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 96 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 97 ==
 * @default
 *
 * @param Region 97 ID
 * @desc The ID of the region that will activate "Camera Region 97"
 * @default
 *
 * @param Region 97 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 97 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 97 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 98 ==
 * @default
 *
 * @param Region 98 ID
 * @desc The ID of the region that will activate "Camera Region 98"
 * @default
 *
 * @param Region 98 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 98 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 98 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 99 ==
 * @default
 *
 * @param Region 99 ID
 * @desc The ID of the region that will activate "Camera Region 99"
 * @default
 *
 * @param Region 99 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 99 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 99 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @param == Camera Region 100 ==
 * @default
 *
 * @param Region 100 ID
 * @desc The ID of the region that will activate "Camera Region 100"
 * @default
 *
 * @param Region 100 Focus
 * @desc The place where the camera is focused.
 * Visit the HELP section for a list of all the available choices.
 * @default
 *
 * @param Region 100 Zoom
 * @desc The scale of the zoom. 1 is a normal scale.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 * @default
 *
 * @param Region 100 Duration
 * @desc The duration in frames it takes to make the screen move to the specified position and scale.
 * @default
 *
 * @help
 *
 * Auto Camera
 * Version 1.02
 * SumRndmDde
 *
 *
 * This plugin requires the Camera Core plugin:
 * http://sumrndm.site/camera-core/
 *
 * This plugin allows developers to set the camera to move based on what regions 
 * the player moves onto.
 *
 *
 * ==============================================================================
 *  Setting up a Camera Region
 * ==============================================================================
 *
 * First, to set up a Camera Region, find a blank new section.
 * Set the "Region X ID" to the ID of the region you wish to use.
 * Whenever the player moves onto that region, the Camera Region will activate.
 *
 * Next, one needs to set up a "Focus", "Zoom", and "Duration".
 *
 *
 *
 * The "Region X Focus" must have one of the following formats:
 *
 *   event [id]
 *
 *   follower [id]
 *
 *   player
 *
 *   shift [x], [y]
 *
 *   [x], [y]
 *
 *
 *
 * The "Region X Zoom" must be set to a zoom be a number around 1.
 * A scale less than 1 zooms out; greater than 1 zooms in.
 *
 *
 *
 * The "Region X Duration" determines the amount of frames it takes to 
 * transition to the new camera... thingy...
 *
 *
 * ==============================================================================
 *  Plugin Commands
 * ==============================================================================
 *
 * If you use commands to change the Zoom or Focus while on a region, you can 
 * reset to the region's current settings using the Plugin Command:
 *
 *   ResetAutoCamera
 *
 *
 * If you wish to change the default zoom used to return the camera back to the 
 * player after leaving a region, use the Plugin Command:
 *
 *   SetResetDuration [duration]
 *
 *
 * ==============================================================================
 *  End of Help File
 * ==============================================================================
 * 
 * Welcome to the bottom of the Help file.
 *
 *
 * Thanks for reading!
 * If you have questions, or if you enjoyed this Plugin, please check
 * out my YouTube channel!
 *
 * https://www.youtube.com/c/SumRndmDde
 *
 *
 * Until next time,
 *   ~ SumRndmDde
 *
 */
/*:ja
 * @plugindesc プレイヤーがいるリージョンでカメラを変化させる設定ができます。
 * @author SumRndmDde
 *
 * @param Reset Duration
 * @text リセット時間
 * @desc 'カメラリージョン'ではないタイルを踏んだ時、ズームを通常に戻すまでのフレーム数
 * @default 30
 *
 * @param == Camera Region 1 ==
 * @text -- カメラリージョン1 --
 * @default
 *
 * @param Region 1 ID
 * @text リージョン1のID
 * @desc 'カメラリージョン1'を有効化するリージョンID
 * @default
 *
 * @param Region 1 Focus
 * @text リージョン1フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 1 Zoom
 * @text リージョン1ズーム
 * @desc ズームの縮尺です1は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 1 Duration
 * @text リージョン1時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 2 ==
 * @text -- カメラリージョン2 --
 * @default
 *
 * @param Region 2 ID
 * @text リージョン2のID
 * @desc 'カメラリージョン2'を有効化するリージョンID
 * @default
 *
 * @param Region 2 Focus
 * @text リージョン2フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 2 Zoom
 * @text リージョン2ズーム
 * @desc ズームの縮尺です2は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 2 Duration
 * @text リージョン2時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 3 ==
 * @text -- カメラリージョン3 --
 * @default
 *
 * @param Region 3 ID
 * @text リージョン3のID
 * @desc 'カメラリージョン3'を有効化するリージョンID
 * @default
 *
 * @param Region 3 Focus
 * @text リージョン3フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 3 Zoom
 * @text リージョン3ズーム
 * @desc ズームの縮尺です3は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 3 Duration
 * @text リージョン3時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 4 ==
 * @text -- カメラリージョン4 --
 * @default
 *
 * @param Region 4 ID
 * @text リージョン4のID
 * @desc 'カメラリージョン4'を有効化するリージョンID
 * @default
 *
 * @param Region 4 Focus
 * @text リージョン4フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 4 Zoom
 * @text リージョン4ズーム
 * @desc ズームの縮尺です4は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 4 Duration
 * @text リージョン4時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 5 ==
 * @text -- カメラリージョン5 --
 * @default
 *
 * @param Region 5 ID
 * @text リージョン5のID
 * @desc 'カメラリージョン5'を有効化するリージョンID
 * @default
 *
 * @param Region 5 Focus
 * @text リージョン5フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 5 Zoom
 * @text リージョン5ズーム
 * @desc ズームの縮尺です5は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 5 Duration
 * @text リージョン5時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 6 ==
 * @text -- カメラリージョン6 --
 * @default
 *
 * @param Region 6 ID
 * @text リージョン6のID
 * @desc 'カメラリージョン6'を有効化するリージョンID
 * @default
 *
 * @param Region 6 Focus
 * @text リージョン6フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 6 Zoom
 * @text リージョン6ズーム
 * @desc ズームの縮尺です6は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 6 Duration
 * @text リージョン6時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 7 ==
 * @text -- カメラリージョン7 --
 * @default
 *
 * @param Region 7 ID
 * @text リージョン7のID
 * @desc 'カメラリージョン7'を有効化するリージョンID
 * @default
 *
 * @param Region 7 Focus
 * @text リージョン7フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 7 Zoom
 * @text リージョン7ズーム
 * @desc ズームの縮尺です7は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 7 Duration
 * @text リージョン7時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 8 ==
 * @text -- カメラリージョン8 --
 * @default
 *
 * @param Region 8 ID
 * @text リージョン8のID
 * @desc 'カメラリージョン8'を有効化するリージョンID
 * @default
 *
 * @param Region 8 Focus
 * @text リージョン8フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 8 Zoom
 * @text リージョン8ズーム
 * @desc ズームの縮尺です8は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 8 Duration
 * @text リージョン8時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 9 ==
 * @text -- カメラリージョン9 --
 * @default
 *
 * @param Region 9 ID
 * @text リージョン9のID
 * @desc 'カメラリージョン9'を有効化するリージョンID
 * @default
 *
 * @param Region 9 Focus
 * @text リージョン9フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 9 Zoom
 * @text リージョン9ズーム
 * @desc ズームの縮尺です9は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 9 Duration
 * @text リージョン9時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 10 ==
 * @text -- カメラリージョン10 --
 * @default
 *
 * @param Region 10 ID
 * @text リージョン10のID
 * @desc 'カメラリージョン10'を有効化するリージョンID
 * @default
 *
 * @param Region 10 Focus
 * @text リージョン10フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 10 Zoom
 * @text リージョン10ズーム
 * @desc ズームの縮尺です10は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 10 Duration
 * @text リージョン10時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 11 ==
 * @text -- カメラリージョン11 --
 * @default
 *
 * @param Region 11 ID
 * @text リージョン11のID
 * @desc 'カメラリージョン11'を有効化するリージョンID
 * @default
 *
 * @param Region 11 Focus
 * @text リージョン11フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 11 Zoom
 * @text リージョン11ズーム
 * @desc ズームの縮尺です11は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 11 Duration
 * @text リージョン11時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 12 ==
 * @text -- カメラリージョン12 --
 * @default
 *
 * @param Region 12 ID
 * @text リージョン12のID
 * @desc 'カメラリージョン12'を有効化するリージョンID
 * @default
 *
 * @param Region 12 Focus
 * @text リージョン12フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 12 Zoom
 * @text リージョン12ズーム
 * @desc ズームの縮尺です12は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 12 Duration
 * @text リージョン12時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 13 ==
 * @text -- カメラリージョン13 --
 * @default
 *
 * @param Region 13 ID
 * @text リージョン13のID
 * @desc 'カメラリージョン13'を有効化するリージョンID
 * @default
 *
 * @param Region 13 Focus
 * @text リージョン13フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 13 Zoom
 * @text リージョン13ズーム
 * @desc ズームの縮尺です13は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 13 Duration
 * @text リージョン13時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 14 ==
 * @text -- カメラリージョン14 --
 * @default
 *
 * @param Region 14 ID
 * @text リージョン14のID
 * @desc 'カメラリージョン14'を有効化するリージョンID
 * @default
 *
 * @param Region 14 Focus
 * @text リージョン14フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 14 Zoom
 * @text リージョン14ズーム
 * @desc ズームの縮尺です14は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 14 Duration
 * @text リージョン14時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 15 ==
 * @text -- カメラリージョン15 --
 * @default
 *
 * @param Region 15 ID
 * @text リージョン15のID
 * @desc 'カメラリージョン15'を有効化するリージョンID
 * @default
 *
 * @param Region 15 Focus
 * @text リージョン15フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 15 Zoom
 * @text リージョン15ズーム
 * @desc ズームの縮尺です15は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 15 Duration
 * @text リージョン15時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 16 ==
 * @text -- カメラリージョン16 --
 * @default
 *
 * @param Region 16 ID
 * @text リージョン16のID
 * @desc 'カメラリージョン16'を有効化するリージョンID
 * @default
 *
 * @param Region 16 Focus
 * @text リージョン16フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 16 Zoom
 * @text リージョン16ズーム
 * @desc ズームの縮尺です16は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 16 Duration
 * @text リージョン16時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 17 ==
 * @text -- カメラリージョン17 --
 * @default
 *
 * @param Region 17 ID
 * @text リージョン17のID
 * @desc 'カメラリージョン17'を有効化するリージョンID
 * @default
 *
 * @param Region 17 Focus
 * @text リージョン17フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 17 Zoom
 * @text リージョン17ズーム
 * @desc ズームの縮尺です17は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 17 Duration
 * @text リージョン17時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 18 ==
 * @text -- カメラリージョン18 --
 * @default
 *
 * @param Region 18 ID
 * @text リージョン18のID
 * @desc 'カメラリージョン18'を有効化するリージョンID
 * @default
 *
 * @param Region 18 Focus
 * @text リージョン18フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 18 Zoom
 * @text リージョン18ズーム
 * @desc ズームの縮尺です18は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 18 Duration
 * @text リージョン18時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 19 ==
 * @text -- カメラリージョン19 --
 * @default
 *
 * @param Region 19 ID
 * @text リージョン19のID
 * @desc 'カメラリージョン19'を有効化するリージョンID
 * @default
 *
 * @param Region 19 Focus
 * @text リージョン19フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 19 Zoom
 * @text リージョン19ズーム
 * @desc ズームの縮尺です19は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 19 Duration
 * @text リージョン19時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 20 ==
 * @text -- カメラリージョン20 --
 * @default
 *
 * @param Region 20 ID
 * @text リージョン20のID
 * @desc 'カメラリージョン20'を有効化するリージョンID
 * @default
 *
 * @param Region 20 Focus
 * @text リージョン20フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 20 Zoom
 * @text リージョン20ズーム
 * @desc ズームの縮尺です20は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 20 Duration
 * @text リージョン20時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 21 ==
 * @text -- カメラリージョン21 --
 * @default
 *
 * @param Region 21 ID
 * @text リージョン21のID
 * @desc 'カメラリージョン21'を有効化するリージョンID
 * @default
 *
 * @param Region 21 Focus
 * @text リージョン21フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 21 Zoom
 * @text リージョン21ズーム
 * @desc ズームの縮尺です21は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 21 Duration
 * @text リージョン21時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 22 ==
 * @text -- カメラリージョン22 --
 * @default
 *
 * @param Region 22 ID
 * @text リージョン22のID
 * @desc 'カメラリージョン22'を有効化するリージョンID
 * @default
 *
 * @param Region 22 Focus
 * @text リージョン22フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 22 Zoom
 * @text リージョン22ズーム
 * @desc ズームの縮尺です22は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 22 Duration
 * @text リージョン22時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 23 ==
 * @text -- カメラリージョン23 --
 * @default
 *
 * @param Region 23 ID
 * @text リージョン23のID
 * @desc 'カメラリージョン23'を有効化するリージョンID
 * @default
 *
 * @param Region 23 Focus
 * @text リージョン23フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 23 Zoom
 * @text リージョン23ズーム
 * @desc ズームの縮尺です23は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 23 Duration
 * @text リージョン23時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 24 ==
 * @text -- カメラリージョン24 --
 * @default
 *
 * @param Region 24 ID
 * @text リージョン24のID
 * @desc 'カメラリージョン24'を有効化するリージョンID
 * @default
 *
 * @param Region 24 Focus
 * @text リージョン24フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 24 Zoom
 * @text リージョン24ズーム
 * @desc ズームの縮尺です24は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 24 Duration
 * @text リージョン24時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 25 ==
 * @text -- カメラリージョン25 --
 * @default
 *
 * @param Region 25 ID
 * @text リージョン25のID
 * @desc 'カメラリージョン25'を有効化するリージョンID
 * @default
 *
 * @param Region 25 Focus
 * @text リージョン25フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 25 Zoom
 * @text リージョン25ズーム
 * @desc ズームの縮尺です25は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 25 Duration
 * @text リージョン25時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 26 ==
 * @text -- カメラリージョン26 --
 * @default
 *
 * @param Region 26 ID
 * @text リージョン26のID
 * @desc 'カメラリージョン26'を有効化するリージョンID
 * @default
 *
 * @param Region 26 Focus
 * @text リージョン26フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 26 Zoom
 * @text リージョン26ズーム
 * @desc ズームの縮尺です26は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 26 Duration
 * @text リージョン26時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 27 ==
 * @text -- カメラリージョン27 --
 * @default
 *
 * @param Region 27 ID
 * @text リージョン27のID
 * @desc 'カメラリージョン27'を有効化するリージョンID
 * @default
 *
 * @param Region 27 Focus
 * @text リージョン27フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 27 Zoom
 * @text リージョン27ズーム
 * @desc ズームの縮尺です27は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 27 Duration
 * @text リージョン27時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 28 ==
 * @text -- カメラリージョン28 --
 * @default
 *
 * @param Region 28 ID
 * @text リージョン28のID
 * @desc 'カメラリージョン28'を有効化するリージョンID
 * @default
 *
 * @param Region 28 Focus
 * @text リージョン28フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 28 Zoom
 * @text リージョン28ズーム
 * @desc ズームの縮尺です28は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 28 Duration
 * @text リージョン28時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 29 ==
 * @text -- カメラリージョン29 --
 * @default
 *
 * @param Region 29 ID
 * @text リージョン29のID
 * @desc 'カメラリージョン29'を有効化するリージョンID
 * @default
 *
 * @param Region 29 Focus
 * @text リージョン29フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 29 Zoom
 * @text リージョン29ズーム
 * @desc ズームの縮尺です29は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 29 Duration
 * @text リージョン29時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 30 ==
 * @text -- カメラリージョン30 --
 * @default
 *
 * @param Region 30 ID
 * @text リージョン30のID
 * @desc 'カメラリージョン30'を有効化するリージョンID
 * @default
 *
 * @param Region 30 Focus
 * @text リージョン30フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 30 Zoom
 * @text リージョン30ズーム
 * @desc ズームの縮尺です30は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 30 Duration
 * @text リージョン30時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 31 ==
 * @text -- カメラリージョン31 --
 * @default
 *
 * @param Region 31 ID
 * @text リージョン31のID
 * @desc 'カメラリージョン31'を有効化するリージョンID
 * @default
 *
 * @param Region 31 Focus
 * @text リージョン31フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 31 Zoom
 * @text リージョン31ズーム
 * @desc ズームの縮尺です31は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 31 Duration
 * @text リージョン31時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 32 ==
 * @text -- カメラリージョン32 --
 * @default
 *
 * @param Region 32 ID
 * @text リージョン32のID
 * @desc 'カメラリージョン32'を有効化するリージョンID
 * @default
 *
 * @param Region 32 Focus
 * @text リージョン32フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 32 Zoom
 * @text リージョン32ズーム
 * @desc ズームの縮尺です32は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 32 Duration
 * @text リージョン32時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 33 ==
 * @text -- カメラリージョン33 --
 * @default
 *
 * @param Region 33 ID
 * @text リージョン33のID
 * @desc 'カメラリージョン33'を有効化するリージョンID
 * @default
 *
 * @param Region 33 Focus
 * @text リージョン33フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 33 Zoom
 * @text リージョン33ズーム
 * @desc ズームの縮尺です33は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 33 Duration
 * @text リージョン33時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 34 ==
 * @text -- カメラリージョン34 --
 * @default
 *
 * @param Region 34 ID
 * @text リージョン34のID
 * @desc 'カメラリージョン34'を有効化するリージョンID
 * @default
 *
 * @param Region 34 Focus
 * @text リージョン34フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 34 Zoom
 * @text リージョン34ズーム
 * @desc ズームの縮尺です34は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 34 Duration
 * @text リージョン34時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 35 ==
 * @text -- カメラリージョン35 --
 * @default
 *
 * @param Region 35 ID
 * @text リージョン35のID
 * @desc 'カメラリージョン35'を有効化するリージョンID
 * @default
 *
 * @param Region 35 Focus
 * @text リージョン35フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 35 Zoom
 * @text リージョン35ズーム
 * @desc ズームの縮尺です35は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 35 Duration
 * @text リージョン35時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 36 ==
 * @text -- カメラリージョン36 --
 * @default
 *
 * @param Region 36 ID
 * @text リージョン36のID
 * @desc 'カメラリージョン36'を有効化するリージョンID
 * @default
 *
 * @param Region 36 Focus
 * @text リージョン36フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 36 Zoom
 * @text リージョン36ズーム
 * @desc ズームの縮尺です36は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 36 Duration
 * @text リージョン36時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 37 ==
 * @text -- カメラリージョン37 --
 * @default
 *
 * @param Region 37 ID
 * @text リージョン37のID
 * @desc 'カメラリージョン37'を有効化するリージョンID
 * @default
 *
 * @param Region 37 Focus
 * @text リージョン37フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 37 Zoom
 * @text リージョン37ズーム
 * @desc ズームの縮尺です37は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 37 Duration
 * @text リージョン37時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 38 ==
 * @text -- カメラリージョン38 --
 * @default
 *
 * @param Region 38 ID
 * @text リージョン38のID
 * @desc 'カメラリージョン38'を有効化するリージョンID
 * @default
 *
 * @param Region 38 Focus
 * @text リージョン38フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 38 Zoom
 * @text リージョン38ズーム
 * @desc ズームの縮尺です38は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 38 Duration
 * @text リージョン38時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 39 ==
 * @text -- カメラリージョン39 --
 * @default
 *
 * @param Region 39 ID
 * @text リージョン39のID
 * @desc 'カメラリージョン39'を有効化するリージョンID
 * @default
 *
 * @param Region 39 Focus
 * @text リージョン39フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 39 Zoom
 * @text リージョン39ズーム
 * @desc ズームの縮尺です39は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 39 Duration
 * @text リージョン39時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 40 ==
 * @text -- カメラリージョン40 --
 * @default
 *
 * @param Region 40 ID
 * @text リージョン40のID
 * @desc 'カメラリージョン40'を有効化するリージョンID
 * @default
 *
 * @param Region 40 Focus
 * @text リージョン40フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 40 Zoom
 * @text リージョン40ズーム
 * @desc ズームの縮尺です40は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 40 Duration
 * @text リージョン40時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 41 ==
 * @text -- カメラリージョン41 --
 * @default
 *
 * @param Region 41 ID
 * @text リージョン41のID
 * @desc 'カメラリージョン41'を有効化するリージョンID
 * @default
 *
 * @param Region 41 Focus
 * @text リージョン41フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 41 Zoom
 * @text リージョン41ズーム
 * @desc ズームの縮尺です41は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 41 Duration
 * @text リージョン41時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 42 ==
 * @text -- カメラリージョン42 --
 * @default
 *
 * @param Region 42 ID
 * @text リージョン42のID
 * @desc 'カメラリージョン42'を有効化するリージョンID
 * @default
 *
 * @param Region 42 Focus
 * @text リージョン42フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 42 Zoom
 * @text リージョン42ズーム
 * @desc ズームの縮尺です42は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 42 Duration
 * @text リージョン42時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 43 ==
 * @text -- カメラリージョン43 --
 * @default
 *
 * @param Region 43 ID
 * @text リージョン43のID
 * @desc 'カメラリージョン43'を有効化するリージョンID
 * @default
 *
 * @param Region 43 Focus
 * @text リージョン43フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 43 Zoom
 * @text リージョン43ズーム
 * @desc ズームの縮尺です43は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 43 Duration
 * @text リージョン43時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 44 ==
 * @text -- カメラリージョン44 --
 * @default
 *
 * @param Region 44 ID
 * @text リージョン44のID
 * @desc 'カメラリージョン44'を有効化するリージョンID
 * @default
 *
 * @param Region 44 Focus
 * @text リージョン44フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 44 Zoom
 * @text リージョン44ズーム
 * @desc ズームの縮尺です44は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 44 Duration
 * @text リージョン44時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 45 ==
 * @text -- カメラリージョン45 --
 * @default
 *
 * @param Region 45 ID
 * @text リージョン45のID
 * @desc 'カメラリージョン45'を有効化するリージョンID
 * @default
 *
 * @param Region 45 Focus
 * @text リージョン45フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 45 Zoom
 * @text リージョン45ズーム
 * @desc ズームの縮尺です45は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 45 Duration
 * @text リージョン45時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 46 ==
 * @text -- カメラリージョン46 --
 * @default
 *
 * @param Region 46 ID
 * @text リージョン46のID
 * @desc 'カメラリージョン46'を有効化するリージョンID
 * @default
 *
 * @param Region 46 Focus
 * @text リージョン46フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 46 Zoom
 * @text リージョン46ズーム
 * @desc ズームの縮尺です46は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 46 Duration
 * @text リージョン46時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 47 ==
 * @text -- カメラリージョン47 --
 * @default
 *
 * @param Region 47 ID
 * @text リージョン47のID
 * @desc 'カメラリージョン47'を有効化するリージョンID
 * @default
 *
 * @param Region 47 Focus
 * @text リージョン47フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 47 Zoom
 * @text リージョン47ズーム
 * @desc ズームの縮尺です47は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 47 Duration
 * @text リージョン47時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 48 ==
 * @text -- カメラリージョン48 --
 * @default
 *
 * @param Region 48 ID
 * @text リージョン48のID
 * @desc 'カメラリージョン48'を有効化するリージョンID
 * @default
 *
 * @param Region 48 Focus
 * @text リージョン48フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 48 Zoom
 * @text リージョン48ズーム
 * @desc ズームの縮尺です48は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 48 Duration
 * @text リージョン48時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 49 ==
 * @text -- カメラリージョン49 --
 * @default
 *
 * @param Region 49 ID
 * @text リージョン49のID
 * @desc 'カメラリージョン49'を有効化するリージョンID
 * @default
 *
 * @param Region 49 Focus
 * @text リージョン49フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 49 Zoom
 * @text リージョン49ズーム
 * @desc ズームの縮尺です49は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 49 Duration
 * @text リージョン49時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 50 ==
 * @text -- カメラリージョン50 --
 * @default
 *
 * @param Region 50 ID
 * @text リージョン50のID
 * @desc 'カメラリージョン50'を有効化するリージョンID
 * @default
 *
 * @param Region 50 Focus
 * @text リージョン50フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 50 Zoom
 * @text リージョン50ズーム
 * @desc ズームの縮尺です50は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 50 Duration
 * @text リージョン50時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 51 ==
 * @text -- カメラリージョン51 --
 * @default
 *
 * @param Region 51 ID
 * @text リージョン51のID
 * @desc 'カメラリージョン51'を有効化するリージョンID
 * @default
 *
 * @param Region 51 Focus
 * @text リージョン51フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 51 Zoom
 * @text リージョン51ズーム
 * @desc ズームの縮尺です51は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 51 Duration
 * @text リージョン51時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 52 ==
 * @text -- カメラリージョン52 --
 * @default
 *
 * @param Region 52 ID
 * @text リージョン52のID
 * @desc 'カメラリージョン52'を有効化するリージョンID
 * @default
 *
 * @param Region 52 Focus
 * @text リージョン52フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 52 Zoom
 * @text リージョン52ズーム
 * @desc ズームの縮尺です52は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 52 Duration
 * @text リージョン52時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 53 ==
 * @text -- カメラリージョン53 --
 * @default
 *
 * @param Region 53 ID
 * @text リージョン53のID
 * @desc 'カメラリージョン53'を有効化するリージョンID
 * @default
 *
 * @param Region 53 Focus
 * @text リージョン53フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 53 Zoom
 * @text リージョン53ズーム
 * @desc ズームの縮尺です53は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 53 Duration
 * @text リージョン53時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 54 ==
 * @text -- カメラリージョン54 --
 * @default
 *
 * @param Region 54 ID
 * @text リージョン54のID
 * @desc 'カメラリージョン54'を有効化するリージョンID
 * @default
 *
 * @param Region 54 Focus
 * @text リージョン54フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 54 Zoom
 * @text リージョン54ズーム
 * @desc ズームの縮尺です54は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 54 Duration
 * @text リージョン54時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 55 ==
 * @text -- カメラリージョン55 --
 * @default
 *
 * @param Region 55 ID
 * @text リージョン55のID
 * @desc 'カメラリージョン55'を有効化するリージョンID
 * @default
 *
 * @param Region 55 Focus
 * @text リージョン55フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 55 Zoom
 * @text リージョン55ズーム
 * @desc ズームの縮尺です55は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 55 Duration
 * @text リージョン55時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 56 ==
 * @text -- カメラリージョン56 --
 * @default
 *
 * @param Region 56 ID
 * @text リージョン56のID
 * @desc 'カメラリージョン56'を有効化するリージョンID
 * @default
 *
 * @param Region 56 Focus
 * @text リージョン56フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 56 Zoom
 * @text リージョン56ズーム
 * @desc ズームの縮尺です56は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 56 Duration
 * @text リージョン56時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 57 ==
 * @text -- カメラリージョン57 --
 * @default
 *
 * @param Region 57 ID
 * @text リージョン57のID
 * @desc 'カメラリージョン57'を有効化するリージョンID
 * @default
 *
 * @param Region 57 Focus
 * @text リージョン57フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 57 Zoom
 * @text リージョン57ズーム
 * @desc ズームの縮尺です57は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 57 Duration
 * @text リージョン57時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 58 ==
 * @text -- カメラリージョン58 --
 * @default
 *
 * @param Region 58 ID
 * @text リージョン58のID
 * @desc 'カメラリージョン58'を有効化するリージョンID
 * @default
 *
 * @param Region 58 Focus
 * @text リージョン58フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 58 Zoom
 * @text リージョン58ズーム
 * @desc ズームの縮尺です58は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 58 Duration
 * @text リージョン58時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 59 ==
 * @text -- カメラリージョン59 --
 * @default
 *
 * @param Region 59 ID
 * @text リージョン59のID
 * @desc 'カメラリージョン59'を有効化するリージョンID
 * @default
 *
 * @param Region 59 Focus
 * @text リージョン59フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 59 Zoom
 * @text リージョン59ズーム
 * @desc ズームの縮尺です59は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 59 Duration
 * @text リージョン59時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 60 ==
 * @text -- カメラリージョン60 --
 * @default
 *
 * @param Region 60 ID
 * @text リージョン60のID
 * @desc 'カメラリージョン60'を有効化するリージョンID
 * @default
 *
 * @param Region 60 Focus
 * @text リージョン60フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 60 Zoom
 * @text リージョン60ズーム
 * @desc ズームの縮尺です60は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 60 Duration
 * @text リージョン60時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 61 ==
 * @text -- カメラリージョン61 --
 * @default
 *
 * @param Region 61 ID
 * @text リージョン61のID
 * @desc 'カメラリージョン61'を有効化するリージョンID
 * @default
 *
 * @param Region 61 Focus
 * @text リージョン61フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 61 Zoom
 * @text リージョン61ズーム
 * @desc ズームの縮尺です61は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 61 Duration
 * @text リージョン61時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 62 ==
 * @text -- カメラリージョン62 --
 * @default
 *
 * @param Region 62 ID
 * @text リージョン62のID
 * @desc 'カメラリージョン62'を有効化するリージョンID
 * @default
 *
 * @param Region 62 Focus
 * @text リージョン62フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 62 Zoom
 * @text リージョン62ズーム
 * @desc ズームの縮尺です62は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 62 Duration
 * @text リージョン62時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 63 ==
 * @text -- カメラリージョン63 --
 * @default
 *
 * @param Region 63 ID
 * @text リージョン63のID
 * @desc 'カメラリージョン63'を有効化するリージョンID
 * @default
 *
 * @param Region 63 Focus
 * @text リージョン63フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 63 Zoom
 * @text リージョン63ズーム
 * @desc ズームの縮尺です63は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 63 Duration
 * @text リージョン63時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 64 ==
 * @text -- カメラリージョン64 --
 * @default
 *
 * @param Region 64 ID
 * @text リージョン64のID
 * @desc 'カメラリージョン64'を有効化するリージョンID
 * @default
 *
 * @param Region 64 Focus
 * @text リージョン64フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 64 Zoom
 * @text リージョン64ズーム
 * @desc ズームの縮尺です64は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 64 Duration
 * @text リージョン64時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 65 ==
 * @text -- カメラリージョン65 --
 * @default
 *
 * @param Region 65 ID
 * @text リージョン65のID
 * @desc 'カメラリージョン65'を有効化するリージョンID
 * @default
 *
 * @param Region 65 Focus
 * @text リージョン65フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 65 Zoom
 * @text リージョン65ズーム
 * @desc ズームの縮尺です65は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 65 Duration
 * @text リージョン65時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 66 ==
 * @text -- カメラリージョン66 --
 * @default
 *
 * @param Region 66 ID
 * @text リージョン66のID
 * @desc 'カメラリージョン66'を有効化するリージョンID
 * @default
 *
 * @param Region 66 Focus
 * @text リージョン66フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 66 Zoom
 * @text リージョン66ズーム
 * @desc ズームの縮尺です66は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 66 Duration
 * @text リージョン66時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 67 ==
 * @text -- カメラリージョン67 --
 * @default
 *
 * @param Region 67 ID
 * @text リージョン67のID
 * @desc 'カメラリージョン67'を有効化するリージョンID
 * @default
 *
 * @param Region 67 Focus
 * @text リージョン67フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 67 Zoom
 * @text リージョン67ズーム
 * @desc ズームの縮尺です67は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 67 Duration
 * @text リージョン67時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 68 ==
 * @text -- カメラリージョン68 --
 * @default
 *
 * @param Region 68 ID
 * @text リージョン68のID
 * @desc 'カメラリージョン68'を有効化するリージョンID
 * @default
 *
 * @param Region 68 Focus
 * @text リージョン68フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 68 Zoom
 * @text リージョン68ズーム
 * @desc ズームの縮尺です68は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 68 Duration
 * @text リージョン68時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 69 ==
 * @text -- カメラリージョン69 --
 * @default
 *
 * @param Region 69 ID
 * @text リージョン69のID
 * @desc 'カメラリージョン69'を有効化するリージョンID
 * @default
 *
 * @param Region 69 Focus
 * @text リージョン69フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 69 Zoom
 * @text リージョン69ズーム
 * @desc ズームの縮尺です69は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 69 Duration
 * @text リージョン69時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 70 ==
 * @text -- カメラリージョン70 --
 * @default
 *
 * @param Region 70 ID
 * @text リージョン70のID
 * @desc 'カメラリージョン70'を有効化するリージョンID
 * @default
 *
 * @param Region 70 Focus
 * @text リージョン70フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 70 Zoom
 * @text リージョン70ズーム
 * @desc ズームの縮尺です70は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 70 Duration
 * @text リージョン70時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 71 ==
 * @text -- カメラリージョン71 --
 * @default
 *
 * @param Region 71 ID
 * @text リージョン71のID
 * @desc 'カメラリージョン71'を有効化するリージョンID
 * @default
 *
 * @param Region 71 Focus
 * @text リージョン71フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 71 Zoom
 * @text リージョン71ズーム
 * @desc ズームの縮尺です71は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 71 Duration
 * @text リージョン71時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 72 ==
 * @text -- カメラリージョン72 --
 * @default
 *
 * @param Region 72 ID
 * @text リージョン72のID
 * @desc 'カメラリージョン72'を有効化するリージョンID
 * @default
 *
 * @param Region 72 Focus
 * @text リージョン72フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 72 Zoom
 * @text リージョン72ズーム
 * @desc ズームの縮尺です72は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 72 Duration
 * @text リージョン72時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 73 ==
 * @text -- カメラリージョン73 --
 * @default
 *
 * @param Region 73 ID
 * @text リージョン73のID
 * @desc 'カメラリージョン73'を有効化するリージョンID
 * @default
 *
 * @param Region 73 Focus
 * @text リージョン73フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 73 Zoom
 * @text リージョン73ズーム
 * @desc ズームの縮尺です73は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 73 Duration
 * @text リージョン73時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 74 ==
 * @text -- カメラリージョン74 --
 * @default
 *
 * @param Region 74 ID
 * @text リージョン74のID
 * @desc 'カメラリージョン74'を有効化するリージョンID
 * @default
 *
 * @param Region 74 Focus
 * @text リージョン74フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 74 Zoom
 * @text リージョン74ズーム
 * @desc ズームの縮尺です74は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 74 Duration
 * @text リージョン74時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 75 ==
 * @text -- カメラリージョン75 --
 * @default
 *
 * @param Region 75 ID
 * @text リージョン75のID
 * @desc 'カメラリージョン75'を有効化するリージョンID
 * @default
 *
 * @param Region 75 Focus
 * @text リージョン75フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 75 Zoom
 * @text リージョン75ズーム
 * @desc ズームの縮尺です75は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 75 Duration
 * @text リージョン75時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 76 ==
 * @text -- カメラリージョン76 --
 * @default
 *
 * @param Region 76 ID
 * @text リージョン76のID
 * @desc 'カメラリージョン76'を有効化するリージョンID
 * @default
 *
 * @param Region 76 Focus
 * @text リージョン76フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 76 Zoom
 * @text リージョン76ズーム
 * @desc ズームの縮尺です76は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 76 Duration
 * @text リージョン76時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 77 ==
 * @text -- カメラリージョン77 --
 * @default
 *
 * @param Region 77 ID
 * @text リージョン77のID
 * @desc 'カメラリージョン77'を有効化するリージョンID
 * @default
 *
 * @param Region 77 Focus
 * @text リージョン77フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 77 Zoom
 * @text リージョン77ズーム
 * @desc ズームの縮尺です77は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 77 Duration
 * @text リージョン77時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 78 ==
 * @text -- カメラリージョン78 --
 * @default
 *
 * @param Region 78 ID
 * @text リージョン78のID
 * @desc 'カメラリージョン78'を有効化するリージョンID
 * @default
 *
 * @param Region 78 Focus
 * @text リージョン78フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 78 Zoom
 * @text リージョン78ズーム
 * @desc ズームの縮尺です78は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 78 Duration
 * @text リージョン78時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 79 ==
 * @text -- カメラリージョン79 --
 * @default
 *
 * @param Region 79 ID
 * @text リージョン79のID
 * @desc 'カメラリージョン79'を有効化するリージョンID
 * @default
 *
 * @param Region 79 Focus
 * @text リージョン79フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 79 Zoom
 * @text リージョン79ズーム
 * @desc ズームの縮尺です79は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 79 Duration
 * @text リージョン79時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 80 ==
 * @text -- カメラリージョン80 --
 * @default
 *
 * @param Region 80 ID
 * @text リージョン80のID
 * @desc 'カメラリージョン80'を有効化するリージョンID
 * @default
 *
 * @param Region 80 Focus
 * @text リージョン80フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 80 Zoom
 * @text リージョン80ズーム
 * @desc ズームの縮尺です80は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 80 Duration
 * @text リージョン80時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 81 ==
 * @text -- カメラリージョン81 --
 * @default
 *
 * @param Region 81 ID
 * @text リージョン81のID
 * @desc 'カメラリージョン81'を有効化するリージョンID
 * @default
 *
 * @param Region 81 Focus
 * @text リージョン81フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 81 Zoom
 * @text リージョン81ズーム
 * @desc ズームの縮尺です81は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 81 Duration
 * @text リージョン81時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 82 ==
 * @text -- カメラリージョン82 --
 * @default
 *
 * @param Region 82 ID
 * @text リージョン82のID
 * @desc 'カメラリージョン82'を有効化するリージョンID
 * @default
 *
 * @param Region 82 Focus
 * @text リージョン82フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 82 Zoom
 * @text リージョン82ズーム
 * @desc ズームの縮尺です82は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 82 Duration
 * @text リージョン82時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 83 ==
 * @text -- カメラリージョン83 --
 * @default
 *
 * @param Region 83 ID
 * @text リージョン83のID
 * @desc 'カメラリージョン83'を有効化するリージョンID
 * @default
 *
 * @param Region 83 Focus
 * @text リージョン83フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 83 Zoom
 * @text リージョン83ズーム
 * @desc ズームの縮尺です83は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 83 Duration
 * @text リージョン83時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 84 ==
 * @text -- カメラリージョン84 --
 * @default
 *
 * @param Region 84 ID
 * @text リージョン84のID
 * @desc 'カメラリージョン84'を有効化するリージョンID
 * @default
 *
 * @param Region 84 Focus
 * @text リージョン84フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 84 Zoom
 * @text リージョン84ズーム
 * @desc ズームの縮尺です84は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 84 Duration
 * @text リージョン84時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 85 ==
 * @text -- カメラリージョン85 --
 * @default
 *
 * @param Region 85 ID
 * @text リージョン85のID
 * @desc 'カメラリージョン85'を有効化するリージョンID
 * @default
 *
 * @param Region 85 Focus
 * @text リージョン85フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 85 Zoom
 * @text リージョン85ズーム
 * @desc ズームの縮尺です85は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 85 Duration
 * @text リージョン85時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 86 ==
 * @text -- カメラリージョン86 --
 * @default
 *
 * @param Region 86 ID
 * @text リージョン86のID
 * @desc 'カメラリージョン86'を有効化するリージョンID
 * @default
 *
 * @param Region 86 Focus
 * @text リージョン86フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 86 Zoom
 * @text リージョン86ズーム
 * @desc ズームの縮尺です86は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 86 Duration
 * @text リージョン86時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 87 ==
 * @text -- カメラリージョン87 --
 * @default
 *
 * @param Region 87 ID
 * @text リージョン87のID
 * @desc 'カメラリージョン87'を有効化するリージョンID
 * @default
 *
 * @param Region 87 Focus
 * @text リージョン87フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 87 Zoom
 * @text リージョン87ズーム
 * @desc ズームの縮尺です87は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 87 Duration
 * @text リージョン87時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 88 ==
 * @text -- カメラリージョン88 --
 * @default
 *
 * @param Region 88 ID
 * @text リージョン88のID
 * @desc 'カメラリージョン88'を有効化するリージョンID
 * @default
 *
 * @param Region 88 Focus
 * @text リージョン88フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 88 Zoom
 * @text リージョン88ズーム
 * @desc ズームの縮尺です88は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 88 Duration
 * @text リージョン88時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 89 ==
 * @text -- カメラリージョン89 --
 * @default
 *
 * @param Region 89 ID
 * @text リージョン89のID
 * @desc 'カメラリージョン89'を有効化するリージョンID
 * @default
 *
 * @param Region 89 Focus
 * @text リージョン89フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 89 Zoom
 * @text リージョン89ズーム
 * @desc ズームの縮尺です89は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 89 Duration
 * @text リージョン89時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 90 ==
 * @text -- カメラリージョン90 --
 * @default
 *
 * @param Region 90 ID
 * @text リージョン90のID
 * @desc 'カメラリージョン90'を有効化するリージョンID
 * @default
 *
 * @param Region 90 Focus
 * @text リージョン90フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 90 Zoom
 * @text リージョン90ズーム
 * @desc ズームの縮尺です90は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 90 Duration
 * @text リージョン90時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 91 ==
 * @text -- カメラリージョン91 --
 * @default
 *
 * @param Region 91 ID
 * @text リージョン91のID
 * @desc 'カメラリージョン91'を有効化するリージョンID
 * @default
 *
 * @param Region 91 Focus
 * @text リージョン91フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 91 Zoom
 * @text リージョン91ズーム
 * @desc ズームの縮尺です91は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 91 Duration
 * @text リージョン91時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 92 ==
 * @text -- カメラリージョン92 --
 * @default
 *
 * @param Region 92 ID
 * @text リージョン92のID
 * @desc 'カメラリージョン92'を有効化するリージョンID
 * @default
 *
 * @param Region 92 Focus
 * @text リージョン92フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 92 Zoom
 * @text リージョン92ズーム
 * @desc ズームの縮尺です92は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 92 Duration
 * @text リージョン92時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 93 ==
 * @text -- カメラリージョン93 --
 * @default
 *
 * @param Region 93 ID
 * @text リージョン93のID
 * @desc 'カメラリージョン93'を有効化するリージョンID
 * @default
 *
 * @param Region 93 Focus
 * @text リージョン93フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 93 Zoom
 * @text リージョン93ズーム
 * @desc ズームの縮尺です93は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 93 Duration
 * @text リージョン93時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 94 ==
 * @text -- カメラリージョン94 --
 * @default
 *
 * @param Region 94 ID
 * @text リージョン94のID
 * @desc 'カメラリージョン94'を有効化するリージョンID
 * @default
 *
 * @param Region 94 Focus
 * @text リージョン94フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 94 Zoom
 * @text リージョン94ズーム
 * @desc ズームの縮尺です94は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 94 Duration
 * @text リージョン94時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 95 ==
 * @text -- カメラリージョン95 --
 * @default
 *
 * @param Region 95 ID
 * @text リージョン95のID
 * @desc 'カメラリージョン95'を有効化するリージョンID
 * @default
 *
 * @param Region 95 Focus
 * @text リージョン95フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 95 Zoom
 * @text リージョン95ズーム
 * @desc ズームの縮尺です95は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 95 Duration
 * @text リージョン95時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 96 ==
 * @text -- カメラリージョン96 --
 * @default
 *
 * @param Region 96 ID
 * @text リージョン96のID
 * @desc 'カメラリージョン96'を有効化するリージョンID
 * @default
 *
 * @param Region 96 Focus
 * @text リージョン96フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 96 Zoom
 * @text リージョン96ズーム
 * @desc ズームの縮尺です96は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 96 Duration
 * @text リージョン96時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 97 ==
 * @text -- カメラリージョン97 --
 * @default
 *
 * @param Region 97 ID
 * @text リージョン97のID
 * @desc 'カメラリージョン97'を有効化するリージョンID
 * @default
 *
 * @param Region 97 Focus
 * @text リージョン97フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 97 Zoom
 * @text リージョン97ズーム
 * @desc ズームの縮尺です97は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 97 Duration
 * @text リージョン97時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 98 ==
 * @text -- カメラリージョン98 --
 * @default
 *
 * @param Region 98 ID
 * @text リージョン98のID
 * @desc 'カメラリージョン98'を有効化するリージョンID
 * @default
 *
 * @param Region 98 Focus
 * @text リージョン98フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 98 Zoom
 * @text リージョン98ズーム
 * @desc ズームの縮尺です98は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 98 Duration
 * @text リージョン98時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 99 ==
 * @text -- カメラリージョン99 --
 * @default
 *
 * @param Region 99 ID
 * @text リージョン99のID
 * @desc 'カメラリージョン99'を有効化するリージョンID
 * @default
 *
 * @param Region 99 Focus
 * @text リージョン99フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 99 Zoom
 * @text リージョン99ズーム
 * @desc ズームの縮尺です99は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 99 Duration
 * @text リージョン99時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @param == Camera Region 100 ==
 * @text -- カメラリージョン100 --
 * @default
 *
 * @param Region 100 ID
 * @text リージョン100のID
 * @desc 'カメラリージョン100'を有効化するリージョンID
 * @default
 *
 * @param Region 100 Focus
 * @text リージョン100フォーカス
 * @desc カメラのピントを合わせる位置
 * 記述方法はヘルプを参照
 * @default
 *
 * @param Region 100 Zoom
 * @text リージョン100ズーム
 * @desc ズームの縮尺です100は通常の縮尺です
 * 縮尺が1より小さいとズームアウト、1より大きいとズームイン
 * @default
 *
 * @param Region 100 Duration
 * @text リージョン100時間
 * @desc 指定した変化が完了するまでのフレーム数
 * @default
 *
 * @help
 * 翻訳:ムノクラ
 * https://fungamemake.com/
 * https://twitter.com/munokura/
 *
 * 元プラグイン: http://sumrndm.site/auto-camera/
 *
 *
 * Auto Camera
 * Version 1.02
 * SumRndmDde
 *
 *
 * このプラグインを使用するには、SRD_CameraCoreプラグインが必要です。
 * http://sumrndm.site/camera-core/
 *
 * このプラグインは、
 * プレイヤーがいるリージョンでカメラを変化させる設定ができます。
 *
 *
 * ==========================================================================
 *  カメラリージョンの設定
 * ==========================================================================
 *
 * まず、カメラリージョンを設定するには、空いている設定箇所を探します。
 * 'Region X ID'を使用したいリージョンのIDに設定します。
 * プレイヤーがそのリージョンに移動する度に、カメラリージョンが起動します。
 *
 * 次に、'Focus'、'Zoom'、'Duration'を設定する必要があります。
 *
 * 'Region X Focus'は、以下の書式で書きます。
 * ※[]は入れません。
 *
 *   event [id]
 *
 *   follower [id]
 *
 *   player
 *
 *   shift [x], [y]
 *
 *   [x], [y]
 *
 *
 * 'Region X Zoom'は、1前後のズームに設定する必要があります。
 * 1未満の場合はズームアウト、1を超える場合はズームインします。
 * 動作させるには、Durationを設定する必要があります。
 *
 * 'Region X Duration'は、
 * 新しいカメラに移行するのにかかるフレーム数を指定します。
 *
 *
 * ==========================================================================
 *  プラグインコマンド
 * ==========================================================================
 *
 * リージョン上でコマンドを使用してズームやフォーカスを変更する場合、
 * プラグインコマンドを使用して
 * リージョンの現在の設定にリセットすることができます。
 *
 *   ResetAutoCamera
 *
 *
 * リージョンを離れた後、
 * カメラをプレーヤーに戻す際に使用するデフォルトのズームを変更したい場合、
 * プラグインコマンドを使用します。
 *
 *   SetResetDuration [duration]
 *
 *
 * ==========================================================================
 *  ヘルプファイルの終わり
 * ==========================================================================
 *
 * ヘルプファイルの終わりへようこそ。
 *
 * 読んでくれてありがとう!
 * 質問があったり、このプラグインを楽しめたら、
 * 私のYouTubeチャンネルを登録してください!!
 *
 * https://www.youtube.com/c/SumRndmDde
 *
 *
 * 次の機会まで
 *   ~ SumRndmDde
 *
 */

var SRD = SRD || {};
SRD.AutoCamera = SRD.AutoCamera || {};

var Imported = Imported || {};
Imported["SumRndmDde Auto Camera"] = 1.02;

(function (_) {

	"use strict";

	//-----------------------------------------------------------------------------
	// SRD.AutoCamera
	//-----------------------------------------------------------------------------

	const params = PluginManager.parameters('SRD_AutoCamera');

	_.meetsRequirements = Imported["SumRndmDde Camera Core"];
	_.defaultDuration = String(params['Reset Duration']);

	_.zooms = [];
	for (let i = 1; i <= 100; i++) {
		let id = String(params['Region ' + i + ' ID']);
		if (id.trim().length > 0) {
			id = parseInt(id);
			_.zooms[id] = {};
			_.zooms[id].focus = String(params['Region ' + i + ' Focus']);
			_.zooms[id].scale = parseFloat(params['Region ' + i + ' Zoom']);
			_.zooms[id].duration = String(params['Region ' + i + ' Duration']);
		}
	}

	_.alertNeedCameraCore = function () {
		alert("The 'SRD_CameraCore' plugin is required for using the 'SRD_AutoCamera' plugin.");
		if (confirm("Do you want to open the download page to 'SRD_CameraCore'?")) {
			window.open('http://sumrndm.site/camera-core/');
		}
	};

	if (!_.meetsRequirements) {
		_.alertNeedCameraCore();
	}

	//-----------------------------------------------------------------------------
	// Game_Screen
	//-----------------------------------------------------------------------------

	const _Game_Screen_clear = Game_Screen.prototype.clear;
	Game_Screen.prototype.clear = function () {
		_Game_Screen_clear.apply(this, arguments);
		this._playerRegionId = -1;
		this._playerDefaultZoom = true;
	};

	const _Game_Screen_updateZoom = Game_Screen.prototype.updateZoom;
	Game_Screen.prototype.updateZoom = function () {
		_Game_Screen_updateZoom.apply(this, arguments);
		this.updateRegionZoom();
	};

	Game_Screen.prototype.updateRegionZoom = function (force) {
		if ((this._playerRegionId !== $gamePlayer.regionId() || !!force) && $dataMap) {
			this._playerRegionId = $gamePlayer.regionId();
			if (_.zooms[this._playerRegionId] || !this._playerDefaultZoom) {
				let info = null;
				if (_.zooms[this._playerRegionId] && this._playerRegionId !== 0) {
					info = _.zooms[this._playerRegionId];
					this._playerDefaultZoom = false;
				} else {
					info = {
						focus: "player",
						scale: SRD.CameraCore.zoom,
						duration: _.defaultDuration
					};
					this._playerDefaultZoom = true;
				}
				this.setupRegionZoom(info);
			}
		}
	};

	Game_Screen.prototype.setupRegionZoom = function (info) {
		const focus = info.focus;
		const scale = info.scale;
		const dur = eval(info.duration);
		this.setCameraFocus(String(info.scale), String(info.duration));
		if (focus.match(/event\s*(\d+)/)) {
			const eventId = parseInt(RegExp.$1);
			if ($gameMap.event(eventId)) {
				this.focusEvent = 9;
				$gameMap.event(eventId).centerCamera(60);
			}
		} else if (focus.match(/follower\s*(\d+)/)) {
			const followerId = parseInt(RegExp.$1);
			if ($gamePlayer.followers().follower(followerId - 1)) {
				this.focusEvent = (followerId * (-1));
				$gamePlayer.followers().follower(followerId - 1).centerCamera(dur);
			}
		} else if (focus === 'player') {
			this.focusEvent = 0;
			$gamePlayer.centerCamera(dur);
		} else if (focus.match(/shift\s*(.+)\s*,\s*(.+)/i)) {
			const coords = focus.match(/shift\s*(.+)\s*,\s*(.+)/i);
			$gameMap.shiftCameraPosition(eval(coords[1]), eval(coords[2]), dur);
		} else {
			const coords = focus.split(/\s*,\s*/);
			this.focusEvent = null;
			$gamePlayer.centerCameraOnPos(eval(coords[0]), eval(coords[1]), dur);
		}
	};

	//-----------------------------------------------------------------------------
	// Game_Interpreter
	//-----------------------------------------------------------------------------

	const _Game_Interpreter_pluginCommand = Game_Interpreter.prototype.pluginCommand;
	Game_Interpreter.prototype.pluginCommand = function (command, args) {
		_Game_Interpreter_pluginCommand.apply(this, arguments);
		if (command.trim().toLowerCase() === 'setresetduration') {
			_.defaultDuration = eval(args[0]);
		} else if (command.trim().toLowerCase() === 'resetautocamera') {
			$gameScreen.updateRegionZoom(true);
		}
	};

})(SRD.AutoCamera);